streamlit>=1.43          # width="stretch" API (replaces use_container_width)
altair>=5.0              # weekly overlap timeline chart
pandas>=2.0              # chart dataframe
SQLAlchemy>=2.0          # dialect-agnostic DB layer (SQLite local / Postgres prod)
psycopg[binary]>=3.1     # Postgres driver (used when DATABASE_URL is set)
tzdata>=2024.1           # zoneinfo tz database (required on Windows)
pytest>=8.0
