# HelpFlow accent only — no base/background override, so each viewer
# keeps Streamlit's built-in Light / Dark / Use-system switch
# (☰ menu → Settings → Theme). Brand blue stays in both modes.
[theme]
primaryColor = "#2EA3F2"
font = "sans serif"
